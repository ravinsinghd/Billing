/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TotalPipe } from './total.pipe';

describe('Pipe: Totale', () => {
  it('create an instance', () => {
    let pipe = new TotalPipe();
    expect(pipe).toBeTruthy();
  });
});
